from django.contrib import admin
from .models import Funcion, Pelicula, Solicitud

# Register your models here.
admin.site.register(Funcion)
admin.site.register(Pelicula)
admin.site.register(Solicitud)
